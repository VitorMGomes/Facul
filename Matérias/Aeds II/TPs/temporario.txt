char* firstAb(Pokemon x)
{
    static char firstAbilitie[60];  // Definimos um buffer estático para armazenar a habilidade
    char *token = strtok(x.abilities, "\'");  // Usamos strtok para separar a primeira habilidade

    if (token != NULL) {
        strcpy(firstAbilitie, token);  // Copiamos a primeira habilidade para o buffer
    }

    return firstAbilitie;  // Retornamos a primeira habilidade
}
