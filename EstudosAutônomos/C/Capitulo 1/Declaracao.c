#include <stdio.h>
#include <stdlib.h>

int main()
{
    int x; //declaração sem definição de valor
    printf("x = %d\n", x);

    x = 5;
    printf("x = %d\n", x);


    return 0;

// %c -- character
// %d ou %i -- int
// %u -- numeros inteiros unsigned
// %f -- float or double
// %s -- varios characteres
// %p -- endereco de memoria
// %e ou %E -- notacao ciencitifica
}